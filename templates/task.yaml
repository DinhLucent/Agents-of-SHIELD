# Frozen Task Contract v2.1

schema_version: "2.1"
id: TASK-2026-001
title: Tighten retry packet context selection for small control-plane tasks
description: >
  Small bugfix in the control plane so retry packets add only targeted context
  after verification failures.
assigned_role: backend
priority: high
status: queued
domain: general

inputs:
  related_paths:
    - control_plane/hooks/on_verification_fail.py
  related_tests: []
  related_handoffs: []
  related_logs: []
  related_modules:
    - control_plane/hooks

constraints:
  - keep_retry_packet_small
  - preserve_existing_retry_contract

acceptance_criteria:
  - retry packet includes only targeted context
  - verification report includes next_context_needs

metadata:
  created_by: CEO
  created_at: "2026-04-11T10:00:00Z"
  # Optional Phase 3 execution contract:
  # execution:
  #   primary_commands:
  #     - "Set-Content -Path 'runtime/sessions/example.txt' -Value 'done' -Encoding utf8"
  #   retry_commands:
  #     - "Set-Content -Path 'runtime/sessions/example.txt' -Value 'retry-done' -Encoding utf8"
  #   output_files:
  #     - runtime/sessions/example.txt
  #   satisfied_criteria:
  #     - retry packet includes only targeted context
  #     - verification report includes next_context_needs
